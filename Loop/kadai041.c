#include<stdio.h>
int main() {
    int gokei = 0, num = 0, heikin = -1;
    while (num != -999) {
        gokei += num;
        heikin++;
        printf("®”(-999‚ÅI—¹)?");
        scanf("%d", &num);
    }
    printf("‡Œv=%d\n", gokei);
    printf("•½‹Ï=%.2f\n", (float)gokei / (float)heikin);
}